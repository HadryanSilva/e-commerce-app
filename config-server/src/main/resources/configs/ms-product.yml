server:
  port: 8091
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=customer_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false